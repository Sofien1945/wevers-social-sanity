/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  //swcMinify: true,
  images: {
    domains: ["weavers.top", "lh3.googleusercontent.com"],
  },
}

module.exports = nextConfig
